[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    ignore::UserWarning
markers =
    real_data: requer CSV INPE + NetCDF GOES locais em data/
    slow: passos mais pesados (ex.: calibração na grelha)
